# 스택과 힙: 메모리는 언제, 어떻게 할당될까?

> **범위:** 이 글의 스택(stack)과 힙(heap)은 **프로그램의 메모리 관리 방식**을 말한다. 자료구조 스택이나 우선순위 큐에 쓰는 힙은 다른 개념이다.

## 1. 프로그램의 메모리 공간

프로그램을 실행하면 운영체제는 프로세스에 **가상 주소 공간**을 제공한다. 이 공간에는 실행 코드, 전역·정적 데이터, 힙, 스택 등이 놓인다. 스택과 힙은 메모리의 *물리적으로 고정된 두 칸*이라기보다, 서로 다른 규칙으로 공간을 할당하고 회수하는 영역이다.

| 영역 | 주로 담는 내용 | 관리 방식 |
| --- | --- | --- |
| 코드 | 실행할 명령어 | 프로그램 실행 파일 등을 메모리에 매핑 |
| 전역·정적 데이터 | 전역 변수, 정적 변수 | 프로그램의 실행 기간과 연결 |
| **스택** | 함수 호출 정보, 일부 지역 변수·임시 값 | 함수 호출이 쌓이고 끝날 때 되돌림 |
| **힙** | 실행 중 동적으로 할당한 데이터 | 필요한 크기만큼 할당하고, 별도로 해제·회수 |

실제 주소 배치, 크기, 사용 방식은 운영체제·언어·컴파일러에 따라 달라진다. 흔한 그림에서 힙은 높은 주소 방향, 스택은 낮은 주소 방향으로 자라지만 **항상 그런 것은 아니다**. 두 영역이 단순히 한 메모리 칸을 양쪽에서 나눠 쓰는 것으로 이해하지 말자.

## 2. 스택이란?

함수를 호출할 때 그 호출에 필요한 정보를 보관하는 영역이다. 각 호출에서 사용하는 공간을 **스택 프레임(stack frame)**이라고 한다. 반환할 위치, 인수, 지역 변수, 임시 값 등이 들어갈 수 있지만, 실제 배치는 최적화에 따라 달라진다.

```text
main() 호출 → A() 호출 → B() 호출
                         B() 종료: B의 프레임 제거
              A() 종료: A의 프레임 제거
```

나중에 호출한 함수가 먼저 끝나므로, 공간을 쌓고 되돌리기 쉽다. **각 스레드는 별도의 스택**을 사용하며, 스택에는 일반적으로 크기 제한이 있다.

## 3. 힙이란?

실행 도중 **필요한 크기의 데이터를 동적으로 할당**할 때 사용하는 영역이다. 함수가 반환해도 계속 필요한 데이터나, 크기가 실행 중 결정되는 데이터에 적합하다.

힙에 할당한 공간은 함수가 끝난다는 이유만으로 자동으로 반환되지 않는다. C에서는 `free` 등으로 해제하고, Python이나 Java에서는 객체를 더 이상 사용하지 않을 때 런타임이 회수하는 방식으로 관리한다. **한 프로세스의 여러 스레드가 힙의 데이터에 접근할 수 있다.**

## 4. 무엇이 어디에 할당될까? — C 예시

```c
#include <stdlib.h>

int global_count = 0;  // 전역·정적 데이터 영역

void example(void) {
    int count = 3;  // 일반적인 지역 변수: 스택 또는 레지스터
    int fixed[3] = {1, 2, 3};  // 지역 배열: 일반적으로 스택

    int *numbers = malloc(count * sizeof *numbers);
    // numbers: 지역 포인터 변수(일반적으로 스택 또는 레지스터)
    // malloc이 확보한 정수 3개 분량의 공간: 힙

    if (numbers == NULL) return;  // 할당 실패 검사

    numbers[0] = fixed[0];
    free(numbers);  // 힙에 할당한 공간 해제
}  // 함수가 끝나면 이 호출의 스택 공간은 되돌려짐
```

`numbers`라는 **포인터 변수**와 그 포인터가 **가리키는 데이터**는 구분해야 한다. 위 코드에서 `free(numbers)`는 포인터 변수 자체가 아니라, 포인터가 가리키는 힙 공간을 해제한다.

> **주의:** “지역 변수 = 무조건 스택”은 정확한 규칙이 아니다. 컴파일러가 지역 값을 레지스터에 둘 수도 있고, 언어나 객체의 생성 방식에 따라 배치가 달라진다. 위 예시는 C의 일반적인 구현을 설명한다.

## 5. 할당과 회수는 어떻게 다를까?

| 비교 | 스택 | 힙 |
| --- | --- | --- |
| 공간 확보 | 함수 호출 등에 따라 프레임을 쌓음 | 할당 요청에 따라 적당한 공간을 찾음 |
| 공간 반환 | 함수 호출이 끝나면 해당 프레임을 되돌림 | 해제 요청 또는 런타임의 회수에 따름 |
| 데이터 수명 | 보통 그 함수 호출이 진행되는 동안 | 함수 호출과 독립적으로 유지 가능 |
| 크기·순서 | 호출 구조에 맞춰 쌓고 되돌림 | 서로 다른 크기의 할당을 서로 다른 시점에 해제 가능 |
| 흔한 문제 | 호출 깊이 증가로 스택 한도 초과 | 메모리 부족, 미해제 데이터 누적, 단편화 |

힙은 수명과 크기가 다른 데이터가 들어오고 나가기 때문에 빈 공간이 흩어지는 **단편화**가 생길 수 있다. 한편 할당된 *가상 주소 공간*의 크기와 실제 사용하는 *물리 메모리*의 크기는 같은 개념이 아니다.

## 6. 스택 오버플로는 언제 발생할까?

함수 호출이 너무 많이 중첩되어 **스택의 사용 한도를 넘을 때** 발생한다. 종료 조건이 없거나 지나치게 깊은 재귀 호출이 대표적인 예다. 함수 안에 지나치게 큰 지역 배열을 두는 것도 원인이 될 수 있다.

```c
void recurse(void) {
    recurse();  // 종료 조건 없이 호출이 계속 쌓임: 실행하지 말 것
}
```

실제 오류의 형태는 환경에 따라 다르다. Python에서는 보통 스택이 완전히 소진되기 전에 재귀 깊이 제한에 걸려 `RecursionError`가 발생한다.

**구별할 점:** 스택 *오버플로*는 스택 공간의 한도를 넘는 일이다. 스택에 놓인 배열의 경계 밖에 쓰는 **스택 버퍼 오버플로**는 별개의 오류다.

## 7. 힙 버퍼 오버플로는 언제 발생할까?

힙에 할당한 **한 버퍼의 범위를 벗어나 데이터를 쓰거나 읽을 때** 발생한다. 특히 C/C++처럼 배열 접근의 경계를 자동으로 확인하지 않는 환경에서 문제가 된다.

예를 들어 `malloc(3 * sizeof(int))`로 정수 3개분의 공간을 확보하면 유효한 인덱스는 `0`, `1`, `2`다. 여기에 `numbers[3] = 10`처럼 쓰면 **힙 버퍼 경계 밖 쓰기**다. 저장된 다른 데이터가 손상되거나 프로그램이 충돌할 수 있다. 이런 코드는 실행하지 않는다.

**힙 버퍼 오버플로 ≠ 힙 메모리 부족.**

| 상황 | 의미 | 예시 |
| --- | --- | --- |
| 힙 버퍼 오버플로 | 이미 할당한 **개별 버퍼의 경계**를 넘음 | 길이 3인 배열의 인덱스 3에 쓰기 |
| 힙 메모리 부족 | **새 공간**을 더 확보하지 못함 | 큰 데이터를 계속 할당하여 `malloc`이 실패 |

Python 리스트는 일반적인 인덱스 접근에서 범위를 검사하므로, `[1, 2, 3][3]`은 힙 버퍼 오버플로가 아니라 `IndexError`를 낸다.

## 핵심 정리

- **스택은 언제 사용되나?** 함수를 호출하면 그 호출을 실행하는 데 필요한 공간인 *스택 프레임*이 쌓인다. 반환할 위치, 일부 지역 변수, 임시 값 등이 여기에 들어갈 수 있다. 함수가 끝나면 해당 호출의 공간을 되돌린다.
- **힙은 언제 사용되나?** 실행 중 데이터를 동적으로 생성·할당할 때 사용한다. C의 `malloc`, C++의 `new`, Python의 리스트 같은 객체 생성이 예다. 힙에 놓인 데이터는 그것을 만든 함수가 끝났다는 이유만으로 바로 사라지지 않으며, 해제하거나 회수하는 방식은 언어마다 다르다.
- **포인터와 실제 데이터는 구분한다.** C에서 함수 안에 `int *p = malloc(100 * sizeof *p);`를 작성했다면, 지역 변수 `p`는 일반적으로 스택(또는 레지스터)에 있고, `malloc`으로 확보한 정수 100개분의 데이터 공간은 힙에 있다.
- **스택 오버플로는 언제 발생하나?** 함수 호출이 지나치게 많이 중첩되어 스택의 크기 제한을 넘을 때 발생한다. 종료 조건이 없는 재귀 호출이 대표적이다. 함수를 호출할 때마다 프레임이 계속 쌓이기 때문이다. 매우 큰 지역 배열도 스택 공간을 소진하는 원인이 될 수 있다.
- **힙 버퍼 오버플로는 언제 발생하나?** 힙에 할당한 **개별 버퍼의 경계**를 넘어 데이터를 쓰거나 읽을 때 발생한다. 예를 들어 정수 3개분의 배열에 인덱스 `3`으로 접근하는 경우다. 이는 힙 전체가 꽉 차서 새 공간을 할당하지 못하는 **힙 메모리 부족**과 다른 문제다.

> **주의:** “지역 변수는 무조건 스택, 객체는 무조건 힙”은 모든 언어에 적용되는 규칙이 아니다. 위의 포인터 예시는 C의 일반적인 메모리 배치를 설명한 것이다.
